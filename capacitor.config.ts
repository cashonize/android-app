import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'xyz.cashtokenswebwallet',
  appName: 'cashtokenswebwallet',
  webDir: 'webDir',
  bundledWebRuntime: false
};

export default config;
